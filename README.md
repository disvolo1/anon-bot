# Deploy on Render

1. Upload these files to a GitHub repository.
2. Create a new **Worker** on Render from that repository.
3. Render will detect `render.yaml`.
4. Add environment variables:
   - BOT_TOKEN
   - ADMIN_ID
5. Deploy.
